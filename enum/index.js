export const TODO_STATUS_ENUM = Object.freeze({
  todo: "todo",
  inProgress: "inProgress",
  completed: "completed",
});

export const TODO_STATUS_ENUM_VALUES = Object.freeze(Object.values(TODO_STATUS_ENUM));

export const USER_ROLES_ENUM = Object.freeze({
  admin: "admin",
  user: "user",
});

export const USER_ROLES_ENUM_VALUES = Object.freeze(Object.values(USER_ROLES_ENUM));
